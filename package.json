{
  "name": "flows_and_functions_of_twilio_flex",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "ci:deploy-functions": "twilio-run deploy --load-system-env --env .env.example --account-sid=$ACCOUNT_SID --auth-token=$AUTH_TOKEN --production --override-existing-project",
    "ci:deploy-flows": "node ./scripts/deployFlows.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "lodash": "^4.17.21",
    "path": "^0.12.7",
    "twilio": "^3.75.0"
  }
}
